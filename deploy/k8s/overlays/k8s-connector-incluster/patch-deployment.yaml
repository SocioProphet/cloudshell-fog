apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudshell-gateway
  namespace: cloudshell-system
spec:
  template:
    spec:
      containers:
        - name: gateway
          env:
            - name: CONNECTOR_MODE
              value: "k8s"
